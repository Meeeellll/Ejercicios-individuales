/* Escriba un programa que muestre la tabla de multiplicar del 1 al 10 del número ingresado por el usuario. */

let tabla = 3;
let i = 0;
let resul;

while(i<10) {
    i++;
    resul = tabla*i;
    console.log(tabla + " X " +i+" = " + resul);
}